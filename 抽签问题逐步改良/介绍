写有数字的卡片有n个，每次盲抽取4张， 抽完后放回。 如果4张卡片之和为m 则输出YES 否则输出NO
卡片的值依次为 k1, k2, k3, ...... kn;

1 <= n <= 50
1 <= m <= 10的八次方
1 <= k <= 10的八次方



（1）
暴力破解：
直接用四次for循环 枚举出全部结果 在循环过程中 只要有一次结果符合要求及 输出
时间复杂度为O(n4)

(2)
二分查找优化
时间复杂度为O(n3 * logn)
在四次循环中进行优化
只要得出 m - k[a] - k[b] - k[c] == k[d]即可得出符合要求
所以对最内层循环进行折半查找

（3）折半查找再次优化
对最内两层循环都做优化 使得k[c] + k[d] == m - k[a] - k[b]即可
构造一个数组 存储每次枚举出的k[c] + k[d]的值 在用折半查找进行优化
复杂度为O(n2 * logn)
